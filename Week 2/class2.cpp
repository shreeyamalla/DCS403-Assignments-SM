#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

int main(){

/*  char letter = 'A';
    // cout << letter << " has code " << int(letter) << endl;
    char next = letter + 1;
    cout << next;
*/

/*  srand(time(0));
    int num = rand() % 26;
    char letter = 'A' + num;
    // char letter = 'A' + rand() % 26;
    cout << letter;
*/

/*  char hex;
    int decimal; 
    cout << "enter a hexadecimal: ";
    cin >> hex;
    if(hex >= '0' && hex <= '9'){
        decimal = hex - '0';
        cout << decimal;
    }else if (hex >= 'A' && hex <= 'F'){
        decimal = 10 + (hex - 'A');
        cout << decimal;
    }
*/

}